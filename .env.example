# Copia para .env local (npm run dev). No GitHub: Settings → Secrets → Actions
VITE_SUPABASE_URL=https://SEU_PROJECTO.supabase.co
VITE_SUPABASE_ANON_KEY=sua_chave_anon_publica

# Só para build GitHub Pages (o workflow define automaticamente pelo nome do repo)
# VITE_BASE_PATH=/sinapse-app/
